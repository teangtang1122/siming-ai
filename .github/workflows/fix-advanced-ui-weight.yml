name: One-shot advanced UI weight compatibility fix

on:
  push:
    branches:
      - feat/android-pc-parity-3
    paths:
      - .github/workflows/fix-advanced-ui-weight.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/android-pc-parity-3
      - run: python .github/scripts/fix_advanced_ui_weight.py
      - run: |
          git rm \
            .github/scripts/fix_advanced_ui_weight.py \
            .github/workflows/fix-advanced-ui-weight.yml
          git add mobile/android/app/src/main/java/com/siming/mobile/ui/AdvancedAuthoringDialogs.kt
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "fix(android): avoid Compose internal weight extension"
          git push origin HEAD:feat/android-pc-parity-3
