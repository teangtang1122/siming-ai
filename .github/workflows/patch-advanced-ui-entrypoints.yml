name: One-shot advanced authoring UI entrypoint patch

on:
  push:
    branches:
      - feat/android-pc-parity-3
    paths:
      - .github/workflows/patch-advanced-ui-entrypoints.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/android-pc-parity-3
      - run: python .github/scripts/patch_advanced_ui_entrypoints.py
      - run: |
          git rm \
            .github/scripts/patch_advanced_ui_entrypoints.py \
            .github/workflows/patch-advanced-ui-entrypoints.yml
          git add \
            mobile/android/app/src/main/java/com/siming/mobile/ui/SimingApp.kt \
            mobile/android/app/src/main/java/com/siming/mobile/ui/AdvancedAuthoringDialogs.kt
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "feat(android): add dedicated chapter and character advanced panels"
          git push origin HEAD:feat/android-pc-parity-3
