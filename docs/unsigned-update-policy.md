# Windows 更新签名策略（临时）

当前项目尚未配置 Windows Authenticode 代码签名证书，因此安装版更新暂时只强制校验 SHA-256，不强制校验 Authenticode 签名。

下载完成后与安装前都会重新计算 SHA-256；任何不匹配都会中止安装。签名校验实现仍保留在代码中，待可信代码签名证书可用后再重新启用强制校验。

3.2.1 与 3.2.2 的客户端已经内置“必须通过签名校验”的旧逻辑，因此首次迁移到取消签名强制校验的版本时需要手动下载安装包一次。